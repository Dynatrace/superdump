@model IEnumerable<SuperDump.Models.SDTag>

@if (Model.Count() > 0) {
	<span class="tags">
		@foreach (var tag in Model.OrderBy(x => x.Name)) {
			<span class="tag tag-@tag.Name">@tag.Name</span>
		}
	</span>
}